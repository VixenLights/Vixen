# Upload FSEQ files directly to ESPixelStick devices (VIX-4003)

This ExecPlan is a living document. Maintain its Progress, Surprises & Discoveries, Decision Log, and Outcomes & Retrospective sections during implementation, following `.agents/PLANS.md` from the repository root. The working directory for every command below is `C:\Dev\Vixen` unless stated otherwise.

## Purpose / Big Picture

A Vixen user who chooses Direct Upload in the FPP export wizard can send generated `.fseq` sequences straight to an ESPixelStick running current firmware, without first saving the export to a folder and uploading it manually. The summary page identifies the device and makes clear that ESPixelStick receives FSEQ files only. A normal Falcon Player host continues to receive sequences, optional audio, and optional universe configuration as before. A successful ESPixelStick export can be seen in the device's sequence list, and a normal FPP export can be checked in its file manager.

After this revision is implemented, an ESPixelStick that reports `zip: true` receives FSEQs in ZIP archives of at most five sequences and one reboot request after every archive upload succeeds. A single sequence also travels in a ZIP on that device. An ESPixelStick that reports `zip: false` or omits `zip` receives individual, unzipped FSEQs through the existing upload path and is not rebooted. Ordinary FPP Direct Upload and File Path export retain their existing behavior.

## Progress

- [x] (2026-09-24 21:41Z) Read VIX-4003, the existing export documentation and code, and the ESPixelStick firmware handlers; established the device marker and request format.
- [x] (2026-09-24 21:50Z) Milestone 1: Updated and re-read the existing VIX-4003 description with user requirements, acceptance criteria, and validation steps; status remains In Progress.
- [x] (2026-09-24 21:59Z) Milestone 2: Added the ESPixelStick FSEQ upload operation and HTTP tests; full-MSBuild test target succeeded, focused tests passed 19/19, and FPP client tests passed 38/38.
- [x] (2026-09-24 22:04Z) Corrected the Milestone 2 unreadable-stream test to use a live write-only stream; the Release test target rebuilt and the upload tests passed 15/15.
- [x] (2026-09-24 22:14Z) Milestone 3: Detect the target once per direct export, share its client/service, route FSEQ uploads, suppress ESPixelStick audio/universe operations, and show FSEQ-only summary text. The Release test target built, focused tests passed 24/24 and the combined FPP client/export-service tests passed 47/47, and Rider found no problems in the changed C# files.
- [x] (2026-09-24 22:32Z) Milestone 4: The user confirmed the full build succeeded, all 980 unit tests passed, and manual validation succeeded. Re-read VIX-4003; its description already matched the implemented behavior and acceptance criteria. Posted the validation results as a Jira comment. The firmware version and exact command output were not provided.
- [x] (2026-09-25 14:17Z) Revised the plan for ESPixelStick ZIP batches and the post-upload reboot; the earlier four milestones remain completed history.
- [x] (2026-09-25 14:23Z) Clarified that both wizard progress bars must show ZIP creation, every archive upload, and the final reboot request without reporting completion early.
- [x] (2026-09-25 14:27Z) Revised the plan so absent or false `zip` uses individual ESPixelStick FSEQ uploads without reboot; the user has a ZIP-capable firmware version available for testing.
- [x] (2026-09-25 14:41Z) Milestone 5: Updated and re-read VIX-4003 with both capability-based routes and acceptance criteria. Verified the live ZIP protocol on 172.20.112.169 (ESPixelStick 4.0-52d8eaf0, `zip: true`): archive upload returned HTTP 200 and matching `Name`, `/X6` returned HTTP 200 with `{"status":"Rebooting"}`, and the device came back online. The probe used a uniquely named empty ZIP, so it verified protocol acknowledgement and reboot recovery, not FSEQ extraction.
- [ ] Milestone 6: Add ESPixelStick ZIP upload and reboot client operations with focused HTTP tests.
- [ ] Milestone 7: Use ZIP batches and one reboot only for ESPixelStick `zip: true`; use the existing individual FSEQ uploads without reboot for absent/false `zip`; make both wizard progress bars follow the selected route and verify both with focused tests.
- [ ] Milestone 8: Run the full validation and hardware acceptance checks, reconcile VIX-4003, and record results.

## Surprises & Discoveries

- The existing FPP client builds HTTP requests under `/api/`. ESPixelStick's firmware instead registers its upload handler at `/fpp` and requires `path=uploadFile` and `filename` parameters; it returns FSEQ metadata JSON, including `Name`, on success. The issue's `/fpp/uploadFile` wording names the operation but is not the literal route registered by current firmware. Evidence: the upstream `WebMgr.cpp` and `FPPDiscovery.cpp` referenced by VIX-4003.
- The wizard already calls `GET /api/system/info` on its summary page, but `ExportDirect` currently constructs a new client for every sequence, and `CreateUniverseFileDirect` constructs another. Device identity must therefore be resolved for the actual export, rather than relying only on the earlier display call.
- Rider's `findTests` coverage hook failed to launch during planning. The existing test targets were located directly at `src/Vixen.Tests/FPPClient/FppClientUploadTests.cs` and `src/Vixen.Tests/ExportWizard/FppDirectUploadServiceTests.cs`. This does not affect the repository's MSBuild and xUnit test route.
- During Milestone 2, a pre-cancelled upload still reached the fake HTTP handler before cancellation surfaced. The first cancellation test incorrectly asserted that no handler call occurred (18 passed, 1 failed). The corrected test requires cancellation propagation without assuming zero handler calls; the focused suite then passed 19/19. The Release build also emitted warnings only in untouched files.
- The unreadable-stream argument test initially disposed a `MemoryStream` and then passed it to the client, a use-after-dispose pattern noted during review. A live `GZipStream` in compression mode has `CanRead == false`, so it exercises the same validation without using a disposed object. The corrected upload tests passed 15/15.
- The summary page's system-info request is only a preview and may precede a host change. Milestone 3 therefore performs a fresh detection before rendering any selected sequence; the preview checks the current host again before updating labels. The existing export loop had no HTTP cancellation token, so a per-run token now cancels active direct-upload requests when the stage is cancelled. The detection contract also requires that a successful response with an empty `Platform` continue through the ordinary FPP route; a missing response or HTTP error stops the run.

- Current upstream firmware advertises `"zip": true` in system info only when built with `SUPPORT_UNZIP`; `FileMgr::StartSdCard` runs `UnzipFiles` on boot when it finds a ZIP and requests another reboot after extraction. The existing Vixen `FppSystemInfo` model does not expose this capability. Evidence: upstream `FPPDiscovery.cpp` (`GetSysInfoJSON`) and `FileMgr.cpp` (`StartSdCard`, `handleFileUpload`). This makes an explicit capability check and hardware check necessary.
- The firmware's `/fpp` POST handler builds an FSEQ-style metadata response even for a ZIP upload, reading the archive header as though it were FSEQ. Its `Name` identifies the staged archive, but other FSEQ metadata must not be used as proof that ZIP entries were extracted. Evidence: upstream `FPPDiscovery.cpp` (`ProcessPOST`, `BuildFseqResponse`).
- The current wizard uploads and deletes each temporary FSEQ inside `ExportDirect`; it must retain successfully rendered files until their batch archive is finalized. `FppDirectUploadService` currently switches between a direct FSEQ request and normal FPP upload; `FppClient` sets its base URL under `/api/`, so both `/fpp` and `/X6` require host-root request paths.

- The wizard has two existing bars: `taskProgress` receives `TaskProgressValue` and `TaskProgressMessage`, while `overallProgress` receives `OverallProgressValue` and `OverallProgressMessage` through `ExportProgressStatus`. `BulkExportSummaryStage.DoExport` currently budgets two overall steps per selected sequence and can therefore reach 100% before new ZIP/upload/reboot work begins. Evidence: `BulkExportSummaryStage.cs` (`DoExport`, `ReportProgress`, `UpdateTaskProgress`, `UpdateOverallProgress`) and `ExportProgressStatus.cs`.

- The user has a verified ZIP-capable firmware version for testing and clarified that older firmware may omit the `zip` field. A missing or false field means ZIP is unsupported, but individual FSEQ uploads remain supported; no reboot is needed for that route. This supersedes the earlier planned unsupported-firmware error.
- The live Milestone 5 probe against 172.20.112.169 reported `Platform: ESPixelStick`, `Version: 4.0-52d8eaf0`, and `zip: true`. A uniquely named empty ZIP upload returned HTTP 200 with matching `Name`; POST `/X6` returned HTTP 200 with `{"status":"Rebooting"}`. The device returned online with the same platform/version and a reset uptime. Because the ZIP had no entries, this confirms the wire protocol and reboot recovery only; it does not establish extraction of actual FSEQ files.

## Decision Log

- Decision: Identify ESPixelStick by an ordinal comparison of `FppSystemInfo.Platform` with `"ESPixelStick"`; all other platform values follow the existing FPP route. Rationale: this is the explicit discriminator supplied for VIX-4003, and `FppSystemInfo` already deserializes `Platform`. Date/Author: 2026-09-24, Codex.
- Decision: For ESPixelStick, upload only FSEQ data; do not upload audio, rename or upload `co-universes.json`, or restart FPPD. Keep the user's saved profile flags intact and explain the effective behavior in the summary. Rationale: the issue says only FSEQ files are uploaded, and the firmware exposes none of those other FPP file/configuration operations. Date/Author: 2026-09-24, Codex.
- Decision: Resolve system info once at the start of each direct export and share one client/service across that export, including multiple sequences. Rationale: this gives a current device identity, avoids repeated calls, and keeps all operations on one target. Failure to identify the device must stop before writing remotely and show the existing upload error dialog. Date/Author: 2026-09-24, Codex.
- Decision: Add a distinct `IFppClient` operation for the ESPixelStick request and keep ordinary `UploadSequenceAsync` unchanged. Rationale: the URL, response shape, and supported operations differ; a separate operation preserves the existing FPP contract and is directly testable. Date/Author: 2026-09-24, Codex.
- Decision: The new upload operation disposes its supplied content stream when the request completes and documents that ownership in its public XML remarks. Rationale: disposing request content also disposes its underlying stream; the wizard's temporary-file stream already has an idempotent disposal scope. Reject path separators in the destination filename before sending. Date/Author: 2026-09-24, Codex.
- Decision: Test unreadable content with a live write-only stream, not a disposed stream. Rationale: the production guard checks `CanRead`, so a live stream with `CanRead == false` tests the intended contract without use-after-dispose. Date/Author: 2026-09-24, Codex.
- Decision: Keep the routing decision in one detected `FppDirectUploadService` per direct-export run, and use its `SupportsFppExtras` property to gate audio and universe actions. Rationale: multiple sequences use one current target identity; the saved profile flags remain intact for a later FPP export. The summary preview uses the same literal platform marker but is not authoritative for the run. An empty or different platform value is ordinary FPP after a successful response. Date/Author: 2026-09-24, Codex.
- Decision: Stop the sequence loop on a direct-upload failure, report Failed rather than Completed, and cancel in-flight HTTP when the user cancels. Rationale: an unsuccessful upload must not appear successful or be followed by unsupported or unrelated remote writes. Date/Author: 2026-09-24, Codex.

- Decision: ESPixelStick Direct Upload always packages FSEQ files in ZIP archives, including a single exported sequence. Preserve selected sequence order and place up to five root-level `.fseq` entries in each archive; the last archive holds one to four entries when needed. Ordinary FPP Direct Upload and File Path output keep their existing formats. Rationale: the new requirement applies only to ESPixelStick, while a target-neutral grouping layer can later serve FPP if its ZIP contract is established. Date/Author: 2026-09-25 14:17Z, Codex.
- Decision: Require an ESPixelStick system-info response that explicitly advertises ZIP support before sending an archive; otherwise stop with a clear unsupported-firmware error. Upload archives sequentially through the ESPixelStick `/fpp?path=uploadFile&filename=...` route and require matching `Name` in each upload response. Do not interpret other FSEQ-style response fields as archive validation. Rationale: upstream firmware compiles unzip support conditionally and processes ZIPs on reboot. Date/Author: 2026-09-25 14:17Z, Codex.
- Decision: After all archive uploads succeed, send one cancellable POST to the host-root `/X6` endpoint and require HTTP success plus JSON `status` equal to `Rebooting`. Never request that reboot after an incomplete run or for ordinary FPP. Treat the response as acknowledgement of reboot initiation; verify extraction after the device returns during hardware acceptance. Rationale: the firmware registers `/X6` for POST and returns that JSON before rebooting. Date/Author: 2026-09-25 14:17Z, Codex.

- Decision: For ESPixelStick runs, include ZIP creation, each archive upload, and the single reboot acknowledgement in the overall progress budget, and show each as a distinct task-bar phase. Keep overall progress monotonic below 100% until `/X6` returns the expected acknowledgement. Reset the task bar at phase start and mark that phase complete only after its operation succeeds; do not fabricate byte-level upload percentages when the HTTP client cannot report bytes sent. Rationale: the existing two-steps-per-sequence calculation would otherwise show a completed export while required device work is still running. Date/Author: 2026-09-25 14:23Z, Codex.

- Decision: Supersede the 2026-09-25 14:17Z decisions that required ZIP for every ESPixelStick run or failed when `zip` was absent/false. Choose the route once from the actual export-run system-info response: only `zip: true` uses ZIP batches and then one `/X6` reboot; absent or false `zip` uses the existing `UploadEspPixelStickSequenceAsync` once per FSEQ and sends no `/X6`. Keep audio and universe suppression for both ESPixelStick routes. Rationale: older firmware can upload individual FSEQs and should remain usable. Date/Author: 2026-09-25 14:27Z, Codex, following user clarification.
- Decision: Apply the ZIP/reboot progress budget only to the `zip: true` route. On absent/false `zip`, retain the current per-sequence progress shape and report completion after the last successful individual upload. Rationale: the wizard must show work that actually occurs on the selected device. Date/Author: 2026-09-25 14:27Z, Codex.
- Decision: Use a uniquely named empty ZIP for the live upload and reboot protocol probe. Rationale: this exercises the archive endpoint and reboot response without overwriting or adding a sequence on the device; it deliberately does not claim FSEQ extraction verification. Date/Author: 2026-09-25, Codex.

## Outcomes & Retrospective

Milestone 1 aligned VIX-4003 with the user-visible FSEQ-only scope, acceptance checks, and validation expectations; the issue remains In Progress. Milestone 2 now provides the separately testable ESPixelStick request in `IFppClient` and `FppClient`, without changing ordinary FPP uploads. It streams an octet-stream body to the host-root handler, checks HTTP success and the returned `Name`, and rejects invalid response metadata. The Release test build succeeded; focused tests passed 19/19 and the FPP client suite passed 38/38. Rider reported no file problems on changed C# lines, and `git diff --check` passed. After review, the unreadable-stream test was corrected to use a live write-only stream; the Release test target rebuilt and the upload tests passed 15/15. Milestone 3 connects the wizard to the new client operation. It detects the target before rendering, shares one client/service for all sequences and any supported FPP extras, and skips audio and universe operations for ESPixelStick without changing saved options or local file-path export. The summary labels now identify ESPixelStick as FSEQ-only and reset on page entry. Detection/upload failure stops the run and shows the existing error dialog; cancellation reaches in-flight HTTP, while temporary FSEQ deletion remains in `finally`. The Release test target built, focused tests passed 24/24 and the combined FPP client/export-service tests passed 47/47, Rider reported no problems on the changed C# files, and `git diff --check` passed. For Milestone 4, the user confirmed the full build succeeded, all 980 unit tests passed, and manual validation succeeded. The current VIX-4003 description already matched the implementation and acceptance criteria; a Jira comment records these results. The firmware version and exact command output were not provided.

The 2026-09-25 revision defines new work after the original four milestones. No ZIP packaging, ZIP capability check, or `/X6` reboot has been implemented or validated yet. The prior 980-test and manual-validation results apply only to the earlier per-FSEQ behavior. The later progress-bar clarification is planned work, not implemented or validated behavior. The user clarified that missing or false `zip` must continue through the already implemented individual FSEQ path without reboot; the ZIP route and capability-based routing remain unimplemented. For Milestone 5, VIX-4003 was updated and re-read with ZIP batch counts, progress, reboot, fallback, error, and unchanged FPP/File Path acceptance. The physical protocol check succeeded on 172.20.112.169 running ESPixelStick firmware 4.0-52d8eaf0: system info reported `zip: true`, the uniquely named empty ZIP upload returned HTTP 200 with matching `Name`, POST `/X6` returned HTTP 200 with `{"status":"Rebooting"}`, and the device returned online with reset uptime. This confirms protocol acknowledgement and reboot recovery only; an actual FSEQ archive extraction check remains for Milestone 8.

## Context and Orientation

The issue is [VIX-4003](https://vixenlights.atlassian.net/browse/VIX-4003), an improvement to FPP export. `docs/export-wizard-fpp-upload.md` defines the current Direct Upload behavior, while `docs/plans/fpp-direct-upload-export-wizard.md` records its implementation. `src/Vixen.Modules/App/ExportWizard/BulkExportOutputFormatStage.cs` collects the host and options. `BulkExportSummaryStage.cs` shows device info and runs the export. `FppDirectUploadService.cs` opens the temporary FSEQ file and delegates uploads. `src/Vixen.Modules/App/FPPClient/Client/FppClient.cs` implements HTTP calls through `IFppClient.cs`; `Models/FppSystemInfo.cs` already contains the `Platform` property. `FppClientOptions.cs` stores the original host URL, but `FppClient` sets `HttpClient.BaseAddress` to the host's `/api/` directory.

An FSEQ file is the binary sequence produced by the export engine. A universe file is the optional FPP output configuration named `co-universes.json`. The existing direct path writes the FSEQ to a temporary local file, uploads it, then deletes it. Preserve that bounded-memory behavior. `FppClientUploadTests.cs` uses a fake HTTP handler to inspect requests without a device; `FppDirectUploadServiceTests.cs` uses Moq to verify client calls. The wizard is legacy WinForms, so maintain its existing controls and event-driven pattern rather than introducing a new view framework.

The firmware sources are `https://github.com/forkineye/ESPixelStick/blob/main/src/WebMgr.cpp` and `https://github.com/forkineye/ESPixelStick/blob/main/src/service/FPPDiscovery.cpp`. `GetSysInfoJSON` sets `Platform` to `ESPixelStick` and `Mode` to `remote` when an SD card is installed. The web server registers POST/PUT at `/fpp`; `ProcessBody` reads `path=uploadFile` and `filename`, writes the request body as file bytes, and `ProcessPOST` returns FSEQ metadata after the file exists. An unavailable SD card causes an HTTP error. Test the wire shape against current firmware before declaring the hardware acceptance complete.

For the new work, `BulkExportSummaryStage.DoExport` owns the sequence order and finalization. Its current `ExportDirect` writes one temporary FSEQ, uploads it immediately, then deletes it. Preserve that existing lifecycle when the detected ESPixelStick has `zip: false` or no `zip` property. Only when `zip: true`, hold a bounded batch of at most five successfully rendered temporary FSEQs until a ZIP is complete, then upload that ZIP and delete both archive and source temporary files in `finally`; flush any remainder after the loop. Use unique, ordered archive names and flat entry names equal to the FSEQ names. Reject duplicate resulting entry names before uploading the affected archive; preflight known names before the first upload. An empty run sends no upload and no reboot. Keep cancellation and upload failure from advancing to another batch or reboot. The existing task and overall progress bars are driven by `ExportProgressStatus`; only the ZIP-capable route needs a new budget for ZIP creation, each archive upload, and reboot.

## Architecture Design: VIX-4003

Detected IDE environment: JetBrains Rider, with the repository's command-line MSBuild route available when a Rider automation hook cannot run. Use Rider's Apply snippet from chat for the small, file-scoped C# updates and its Test Runner for focused tests where available; use the exact commands below as the reliable fallback. The design follows the project `dotnet-best-practices`, `csharp-async`, and `csharp-docs` skills: keep asynchronous HTTP I/O cancellable, dispose streams/responses, use structured NLog calls, and document the new public interface method. The existing `IFppClient` and `FppClientFactory` remain the provider boundary; no new persisted data model, dependency injection registration, or project is required.

The routing predicate is `string.Equals(info.Platform, "ESPixelStick", StringComparison.Ordinal)`. A missing or different value uses FPP behavior only after a successful system-info response; a failed or malformed response aborts direct export rather than guessing. For ESPixelStick, send `POST` to the host-root URL `/fpp?path=uploadFile&filename=<URI-escaped-fseq-name>` with the FSEQ bytes as `application/octet-stream`. The leading slash is essential because the current `HttpClient.BaseAddress` ends in `/api/`. Use the configured upload timeout and caller cancellation token. Treat a non-2xx response as `FppClientException` with the status and filename. On HTTP success, read the firmware metadata response and verify its `Name` is the requested filename; an empty, invalid, or mismatched response is a failed upload, not a reported success. The firmware currently returns this metadata from `BuildFseqResponse`; confirm the exact body in the hardware acceptance check.

The component boundary is: `BulkExportSummaryStage` fetches current system info before starting the sequence loop and shows the effective ESPixelStick limitations; `FppDirectUploadService` routes a generated FSEQ according to the resolved target type; `FppClient` performs the device-specific HTTP request. `BulkExportOutputFormatStage` continues collecting the same saved options. `ExportProfile` serialization is unchanged. The normal file-path export never queries a device.

The new protocol extends the completed per-FSEQ implementation. Extend `FppSystemInfo` to deserialize the firmware's optional `zip` Boolean; missing and false both select the existing individual ESPixelStick FSEQ route. The detected service selects the route once per export from `info.Zip == true`; a failed or malformed system-info response still aborts before uploading. Only the ZIP route builds disk-backed archives with `System.IO.Compression.ZipArchive`, copies file streams rather than buffering entire sequences, and invokes the new archive and reboot client operations. Keep packaging behind a target-capability policy so FPP could use it in the future only after its contract is verified; current FPP behavior does not change.

## Plan of Work

### Milestone 1 — Align the issue before implementation

Update the existing VIX-4003 description, rather than creating a second issue. Keep the issue user-facing: state that a user can select an ESPixelStick host in Direct Upload, that Vixen detects it from system info, that the resulting FSEQ appears on the device, and that audio and universe configuration are not uploaded to ESPixelStick. State that ordinary FPP and file-path exports retain their current behavior. Include acceptance checks for one sequence, multiple sequences, an ESPixelStick with audio/universe options selected, an unreachable or storage-less target, a normal FPP host, and file-path export. Include automated test and hardware validation expectations in plain language. Re-read VIX-4003 to verify the update; do not transition its status merely to satisfy this plan.

### Milestone 2 — Implement the device-specific client request

In `src/Vixen.Modules/App/FPPClient/Client/IFppClient.cs`, declare `Task UploadEspPixelStickSequenceAsync(string filename, Stream content, CancellationToken cancellationToken = default)` with XML summary, parameter, exception, and return documentation. In `src/Vixen.Modules/App/FPPClient/Client/FppClient.cs`, implement it using the existing `_httpClient`, `_options.UploadTimeout`, and structured NLog pattern. Validate the filename and stream, escape the filename as a query value, use a host-root request URI, and send a streaming octet-stream body without buffering the entire file. Dispose linked cancellation, request content, and response correctly; the service closes its temporary file stream after the call. Check HTTP status and the returned `Name`; surface a clear `FppClientException` for an unsuccessful response. Leave `UploadSequenceAsync`, `UploadFileAsync`, and other FPP endpoints unchanged.

Extend `src/Vixen.Tests/FPPClient/FppClientUploadTests.cs` using its existing fake HTTP handler. The handler in `src/Vixen.Tests/FPPClient/Helpers/MockHttpMessageHandler.cs` accepts an asynchronous callback so a test can inspect request bytes without blocking. Assert the full host-root path and query, POST method, content type, exact body bytes, successful metadata handling, escaped filenames, cancellation/error behavior, and rejection of a mismatched or invalid metadata response. Keep normal-FPP upload tests green. A focused Rider Test Runner execution or the test command below proves this milestone independently.

### Milestone 3 — Route the wizard export and explain effective options

In `src/Vixen.Modules/App/ExportWizard/BulkExportSummaryStage.cs`, fetch `GetSystemInfoAsync` once for the actual direct-export run before rendering/uploading any sequence. Share the resulting client and `FppDirectUploadService` through the sequence loop and the optional universe step, instead of creating a client for each item. Dispose the client after the run. If detection fails, log and display the existing direct-upload error and avoid remote writes. Handle the user's cancellation consistently with the current wizard. Pass the resolved ESPixelStick predicate to the service; in `FppDirectUploadService.UploadSequenceFileAsync`, call the new client operation for ESPixelStick and the existing `UploadSequenceAsync` for FPP. Do not change sequence rendering or local file-path export.

For an ESPixelStick run, bypass the audio branch in `ExportDirect` and the universe backup/upload/restart branch in `CreateUniverseFile`. Ensure the per-sequence progress still reaches completion and temporary FSEQ files are deleted on success, error, or cancellation. Do not overwrite saved `IncludeAudio`, `CreateUniverseFile`, or `BackupUniverseFile` flags: a later export to ordinary FPP should still honor them. In `ConfigureSummary`/`PopulateFppInfoAsync`, show `Platform: ESPixelStick` and a clear FSEQ-only notice, and ensure audio/universe summary labels do not claim those files will be uploaded. Restore the ordinary summary text on each page entry so switching hosts does not leave a stale notice. Keep any changed public/protected API XML comments accurate.

Extend `src/Vixen.Tests/ExportWizard/FppDirectUploadServiceTests.cs` to verify both routing branches and progress/error behavior. Extract only the small decision logic needed for unit coverage from the WinForms stage; test that ESPixelStick suppresses audio and universe actions while a normal FPP target retains them. Verify a system-info failure prevents all upload calls. Keep the diff limited to this behavior and run Rider `get_file_problems` on each changed C# file; fix only findings on changed lines.

### Milestone 4 — Validate and close out the plan

Build the affected solution and run the test project with the full MSBuild C++ toolchain, then run the focused and full xUnit suites without rebuilding. In Rider, the Test Runner can repeat the focused tests; the `findTests` dotCover hook failed during planning, so it is optional. Manually export at least one FSEQ and then multiple FSEQs to an SD-equipped ESPixelStick on current firmware; verify their names and contents in the device sequence list and confirm no audio or `co-universes.json` action was attempted. Repeat with an ordinary FPP host, with file-path output, and with an inaccessible or storage-less device to verify the visible error. Record firmware version, commands, pass counts, and any missing hardware check in this plan. Reconcile VIX-4003's description if the final behavior differs from the initial requirements, then add a concise Jira comment with validation results. Do not claim hardware verification unless a device was actually exercised.

### Milestone 5 — Align the issue and establish the ZIP protocol

Update the existing VIX-4003 description with both ESPixelStick routes. For `zip: true`, describe one ZIP even for a single FSEQ, ordered groups of five for multiple FSEQs, one shorter final group, one `/X6` reboot after all archives succeed, and progress bars that show ZIP creation, uploads, and reboot. For `zip: false` or a missing `zip` property, describe existing individual FSEQ uploads and no reboot. Add acceptance checks for 1, 5, 6, 10, and 11 sequences on the ZIP route; one and multiple sequences on both non-ZIP variants; upload/reboot errors; normal FPP; and File Path output. Re-read the issue to verify the update. Use the user's verified ZIP-capable firmware version for the protocol check when available: confirm `GET /api/system/info` exposes `zip: true`, a `.zip` posted to `/fpp?path=uploadFile&filename=<archive>.zip` returns a matching `Name`, and POST `/X6` returns `{"status":"Rebooting"}`. Record firmware build/version and evidence. A fake handler can establish the missing/false fallback without requiring an older physical device. Do not infer extracted FSEQs merely from the ZIP upload response. If deployed ZIP-capable firmware behaves differently, update this plan before source changes.

**STOP BOUNDARY:** Stop execution, run `git diff` on the modified files, invoke the `commit-msg` skill with the VIX-4003 ticket prefix, and wait for explicit human review.

### Milestone 6 — Add archive and reboot client contracts

In `src/Vixen.Modules/App/FPPClient/Models/FppSystemInfo.cs`, add an optional Boolean for the firmware's lowercase `zip` system-info field and document its public meaning, including the missing-field case. In `src/Vixen.Modules/App/FPPClient/Client/IFppClient.cs` and `FppClient.cs`, add a public ESPixelStick archive upload operation that accepts an archive filename and readable stream, uses the configured upload timeout and cancellation token, posts ZIP bytes to the host-root `/fpp?path=uploadFile&filename=<escaped-name>`, rejects non-success HTTP and invalid or mismatched `Name`, and disposes request resources. Reuse the current ESPixelStick transport logic where practical while retaining the existing FSEQ method as the required path when `zip` is absent or false. Add a separate public asynchronous ESPixelStick reboot operation: POST host-root `/X6` once with no payload, require a successful HTTP status and a JSON object whose string `status` is exactly `Rebooting`, and report malformed or unexpected responses as `FppClientException`. Document cancellation, errors, and stream ownership in XML comments. Keep all normal FPP client methods unchanged.

Extend `src/Vixen.Tests/FPPClient/FppClientGetSystemInfoTests.cs` and `FppClientUploadTests.cs` with fake-handler cases for `zip: true`, `zip: false`, and absent `zip` deserialization; exact ZIP request URI/body/name handling; escaping; HTTP and metadata errors; exact reboot method/path/body/response; cancellation; and normal FPP behavior. Run the focused FPP client tests using the commands below. The expected result is that new tests pass and the existing FPP tests remain green.

**STOP BOUNDARY:** Stop execution, run `git diff` on the modified files, invoke the `commit-msg` skill with the VIX-4003 ticket prefix, and wait for explicit human review.

### Milestone 7 — Batch ESPixelStick FSEQs and finish with one reboot

In `src/Vixen.Modules/App/ExportWizard/FppDirectUploadService.cs`, retain the detected ESPixelStick identity and add its ZIP capability from the same system-info response. Expose ESPixelStick archive-upload and reboot calls while preserving `UploadSequenceFileAsync` for the absent/false capability route. Treat the batch size as an ESPixelStick ZIP policy, isolated from ordinary FPP so the same packaging mechanism could be enabled later only after an FPP ZIP contract is verified. In `src/Vixen.Modules/App/ExportWizard/BulkExportSummaryStage.cs`, preserve selected sequence order. Only for ESPixelStick `zip: true`, render each FSEQ to a temporary file, add each successful result to a pending batch, and create a disk-backed ZIP when the batch reaches five; flush a final batch of one to four after the loop. For one selected sequence on that route, create and upload a one-entry ZIP. Each ZIP contains only flat `.fseq` entries with the exact destination names. Use unique archive names with ordered part numbers; reject duplicate entry names before uploading the affected archive and preflight known names before the first upload. Upload batches sequentially and request a single reboot only after every intended archive is acknowledged. For ESPixelStick with absent or false `zip`, call the existing individual-FSEQ upload path once per successfully exported sequence, delete each temporary FSEQ as today, and never call the archive-upload or reboot methods. Make the wizard summary reflect the detected route: ZIP batches followed by reboot for `zip: true`, or individual FSEQ uploads with no reboot for absent/false `zip`; keep the FSEQ-only audio/universe notice on both. On failed render, upload, cancellation, or ZIP creation, stop without reboot and show the existing failure/cancellation state. If the user elects to skip an unreadable sequence, do not include it; if none remain, do not reboot. Delete all owned temporary FSEQ and ZIP files on every path, including an incomplete batch. Keep the existing FPP per-sequence upload, audio, universe, restart, and File Path branches unchanged. Update the existing wizard progress reporting in `BulkExportSummaryStage.DoExport` and, where appropriate, `FppDirectUploadService` so both bars cover the final device work. For an ESPixelStick `zip: true` run, budget render/export work plus one ZIP-creation and one upload step for each planned archive and one reboot step; account for skipped sequences and the actual final batch without moving the overall bar backwards or reaching 100% early. For absent/false `zip`, retain the existing per-sequence upload progress and reach 100% after the final successful individual upload, with no ZIP or reboot labels. Before each archive, reset the task bar to 0 with `Creating ZIP <part>/<total>`; advance it as entries are added and mark 100 only when that ZIP is closed successfully. Reset it to 0 with `Uploading ZIP <part>/<total>` for the HTTP request and mark 100 only after the matching upload response; if byte-level transfer progress is unavailable, leave it at 0 while the request is in flight rather than inventing a percentage. Reset it to 0 with `Requesting ESPixelStick reboot` and mark 100 only after `/X6` returns `Rebooting`. Advance the overall bar after each successful phase; set it to 100 and label it `Completed` only after that reboot acknowledgement. On failure or cancellation, retain the last earned overall percentage and show `Failed` or `Cancelled`, never `Completed`. Preserve the existing FPP and File Path progress behavior and do not introduce new controls.

Add focused batch tests in `src/Vixen.Tests/ExportWizard/` for archive entry names and bytes, order, counts 1/5/6/10/11, cleanup, duplicate names, upload failure on a later batch, cancellation, no reboot on partial failure, and exactly one reboot after the last successful archive. Add routing tests for `zip: true`, `zip: false`, and an absent `zip` field: the latter two call `UploadEspPixelStickSequenceAsync` for every FSEQ, create no ZIPs, and never call `/X6`, including for multiple sequences and after successful completion. Test the emitted `ExportProgressStatus` sequence for a one-archive and a multi-archive run: ZIP, upload, and reboot task labels appear in order; each task phase starts at 0 and completes only on success; overall values never decrease or reach 100 before reboot acknowledgement; failure/cancellation never displays `Completed`. Include absent/false `zip` progress assertions showing per-sequence completion without ZIP/reboot stages, and a normal FPP assertion that its progress route remains unchanged. Extend `FppDirectUploadServiceTests.cs` to prove normal FPP still calls the original sequence method and never ZIP upload or `/X6`. Run file-problem checks for changed C# files and fix only findings on changed lines; run focused tests and an affected Release build.

**STOP BOUNDARY:** Stop execution, run `git diff` on the modified files, invoke the `commit-msg` skill with the VIX-4003 ticket prefix, and wait for explicit human review.

### Milestone 8 — Validate ZIP extraction and record completion

Run the full Release solution build and the full test workflow below. Manually export disposable batches of 1, 5, 6, 10, and 11 sequences to an SD-equipped ESPixelStick with `zip: true`; observe exactly 1, 1, 2, 2, and 3 ZIP uploads respectively, then one POST `/X6` per successful export. Watch both wizard progress bars during those runs: the task bar must visibly show each ZIP creation and upload plus the reboot request, while the overall bar must continue advancing through those phases and reach 100% only after the reboot acknowledgement. After the device returns and finishes extraction, verify every named FSEQ appears with correct contents in its sequence list. Exercise failed upload, unavailable storage, and cancellation to confirm no reboot is requested. Also test an ESPixelStick system-info response with `zip: false` and one with `zip` absent: each must upload every selected FSEQ individually and finish without `/X6`; a fake-handler test is sufficient if an older device is unavailable. Then verify normal FPP and File Path exports still work as before. Record firmware version, exact commands, pass counts, and any hardware checks that could not be performed. Reconcile VIX-4003's description with implemented behavior and add a Jira comment containing the validation results; do not claim hardware verification without a device.

**STOP BOUNDARY:** Stop execution, run `git diff` on the modified files, invoke the `commit-msg` skill with the VIX-4003 ticket prefix, and wait for explicit human review.

## Concrete Steps

From `C:\Dev\Vixen`, inspect `git status --short` before editing and preserve unrelated changes. For Milestone 2, run the full-MSBuild test target first because two test dependencies are C++/CLI projects. The commands are:

    msbuild Vixen.sln -m -restore -t:Vixen_Tests -p:Configuration=Release -p:Platform=x64 -p:PlatformTarget=x64 -v:m
    dotnet test src/Vixen.Tests/Vixen.Tests.csproj -c Release --no-build --no-restore -p:Platform=x64 -p:SolutionDir="C:/Dev/Vixen/" --filter "FullyQualifiedName~FppClientUploadTests|FullyQualifiedName~FppDirectUploadServiceTests"
    dotnet test src/Vixen.Tests/Vixen.Tests.csproj -c Release --no-build --no-restore -p:Platform=x64 -p:SolutionDir="C:/Dev/Vixen/"

When production changes span both modules, also run:

    msbuild Vixen.sln -m -t:restore -t:Rebuild -p:Configuration=Release

Record the actual passed/failed totals rather than assuming a fixed future count. The focused tests should fail before the client/routing change and pass after it; the existing upload and export tests must remain passing. Launch the built application via Rider's Vixen run configuration for manual wizard checks, or use the application's executable from `Release/Output/` if that configuration is unavailable. In the wizard select Falcon Pi Player 2.x format, Direct Upload, and the target host; finish the export and inspect the device. Use a disposable test sequence and host so repeated uploads do not overwrite show data.

For Milestones 5–8, work from `C:\Dev\Vixen`. Before any source edit, run `git status --short` and read the target files. Use a disposable device and sequences for the protocol probe. The new focused client and export tests use the repository's full-MSBuild test target first, because the test project depends on C++/CLI projects. Run:

    msbuild Vixen.sln -m -restore -t:Vixen_Tests -p:Configuration=Release -p:Platform=x64 -p:PlatformTarget=x64 -v:m
    dotnet test src/Vixen.Tests/Vixen.Tests.csproj -c Release --no-build --no-restore -p:Platform=x64 -p:SolutionDir="C:/Dev/Vixen/" --filter "FullyQualifiedName~FppClientUploadTests|FullyQualifiedName~FppClientGetSystemInfoTests|FullyQualifiedName~FppDirectUploadServiceTests|FullyQualifiedName~EspPixelStickArchive"
    dotnet test src/Vixen.Tests/Vixen.Tests.csproj -c Release --no-build --no-restore -p:Platform=x64 -p:SolutionDir="C:/Dev/Vixen/"
    msbuild Vixen.sln -m -t:restore -t:Rebuild -p:Configuration=Release

The new tests should fail before their implementation and pass after it; record actual counts. The earlier 980-test result is historical, not an expected new count. For the manual check, use Rider's Vixen run configuration or the built executable in `Release/Output/`, select Falcon Pi Player 2.x and Direct Upload, and inspect HTTP requests and the device's sequence list. Wait for reboot and extraction only on the `zip: true` route; an absent/false `zip` route must not request a reboot. For each new milestone, record progress and evidence in this plan before its STOP BOUNDARY.

## Validation and Acceptance

Given an ESPixelStick whose `GET /api/system/info` returns `Platform: "ESPixelStick"`, a Direct Upload of `Test.fseq` sends one streamed FSEQ request to the firmware upload handler, reports completion only after a successful response, and leaves `Test.fseq` on the device. Multiple selected sequences produce one upload per sequence while system info is resolved once for the export run. With Include Audio and universe options selected, the summary explicitly states that these files are not uploaded and no music, configuration, rename, or FPPD restart HTTP request occurs. If system info or file storage is unavailable, the user sees an actionable error and the wizard does not report a successful FSEQ upload. The temporary FSEQ is removed in every outcome.

Given a normal FPP system-info response, the sequence still uses `/api/file/sequences/<name>`, and selected audio and universe options still use their existing FPP operations. Given File Path mode, exports still write to the selected folder without contacting the host. The Release solution build and focused/full xUnit commands above must pass; capture their actual counts. A real ESPixelStick check is required to establish protocol compatibility because a fake HTTP handler cannot prove firmware routing or storage behavior.

The following capability-based requirements supersede earlier unconditional ZIP language. The original per-FSEQ ESPixelStick behavior remains the fallback when `zip` is missing or false. With `Platform: "ESPixelStick"` and `zip: true`, a single successful FSEQ export sends one one-entry ZIP; 5 sends one five-entry ZIP; 6 sends batches of 5 and 1; 10 sends 5 and 5; 11 sends 5, 5, and 1. Every ZIP entry is a root-level `.fseq` with the exported name and unchanged file bytes. The archive uploads use host-root `/fpp`, in batch order, with matching response `Name`; only after the final successful response does one host-root POST `/X6` occur. Its HTTP-success JSON response has `status: "Rebooting"`. Following reboot and extraction, all exported FSEQs appear on the device. During that run, the task bar labels and values show each archive being created, then uploaded, followed by the reboot request; an in-flight upload or reboot stays incomplete until acknowledged. The overall bar advances for each completed phase, never falls back, and cannot show 100% or `Completed` before the reboot acknowledgement. If ZIP creation, upload, or reboot fails or is cancelled, both bars stop at their last earned progress and the overall status reads `Failed` or `Cancelled`. The ordinary FPP and File Path progress behavior remains unchanged. Upload or reboot response errors must show failure, and incomplete/cancelled ZIP runs must not request reboot. If `zip` is missing or false, each successfully exported FSEQ is sent individually through `/fpp?path=uploadFile&filename=<fseq-name>` with its original bytes, no ZIP is created, the task/overall bars follow the current per-sequence path, and `/X6` is never called. Completion on that route follows the final successful individual upload. An empty successful set causes no upload and no reboot.

For ordinary FPP, every sequence still uses its original `/api/file/sequences/<name>` upload with optional audio and universe operations, and no ZIP or `/X6` request. File Path output still writes individual files locally and never contacts a host. Validate these unchanged branches with automated tests and a manual export. The HTTP handler tests establish request shape and control flow; the real-device check establishes whether this firmware extracts all archives after the reboot sequence.

## Idempotence and Recovery

Re-running the tests and builds is safe. Re-running a manual direct export may replace an identically named sequence on the target; use a throwaway sequence or back up device content first. A failed upload must leave the original local sequence untouched and clean its temporary export file. If a network/device check fails, fix connectivity or storage and retry the wizard; do not fall back silently to the normal FPP endpoint. Restore the user's existing profile selections when switching to another host. Do not create a Git commit unless explicitly requested.

New ZIP archives must be temporary and uniquely named per run so retries do not collide with earlier staged archives. If an upload fails after earlier archives succeeded, report the partial remote state and retain no local temporary files; do not reboot automatically. Because the device may reboot before a client can observe the `/X6` response, treat an absent or malformed acknowledgement as a reported failure with uncertain remote reboot state and direct the user to inspect the device before retrying. Do not add automatic retries or deletion of remote files without a verified firmware contract. Use an ESPixelStick with a disposable SD card for repeated checks, since uploading archive entries may overwrite matching existing FSEQs after extraction.

## Artifacts and Notes

Expected ESPixelStick wire shape for `zip: false` or missing `zip`, with the filename escaped as a URI query component; repeat the POST once per exported FSEQ and do not POST `/X6`:

    GET  http://<host>/api/system/info  -> {"Platform":"ESPixelStick", ...}
    POST http://<host>/fpp?path=uploadFile&filename=Test.fseq
    Content-Type: application/octet-stream
    Body: the generated FSEQ bytes
    Response: HTTP 200, JSON with "Name":"Test.fseq"

The ticket retains links to the firmware sources above. The route and parameter interpretation comes from those sources; the final hardware check validates the complete request against an actual firmware build. The existing FPP route is `POST http://<host>/api/file/sequences/Test.fseq` and must remain unchanged.

Expected additional ESPixelStick wire sequence for a six-FSEQ example; archive names are illustrative and must be unique within a run:

    GET  http://<host>/api/system/info  -> {"Platform":"ESPixelStick","zip":true,...}
    POST http://<host>/fpp?path=uploadFile&filename=vixen-export-<run>-part-001.zip
    Content-Type: application/octet-stream
    Body: ZIP with five root-level .fseq entries
    Response: HTTP 200, JSON with "Name":"vixen-export-<run>-part-001.zip"
    POST http://<host>/fpp?path=uploadFile&filename=vixen-export-<run>-part-002.zip
    Body: ZIP with one root-level .fseq entry
    Response: HTTP 200, JSON with matching "Name"
    POST http://<host>/X6
    Response: HTTP 200, {"status":"Rebooting"}

The `/X6` response proves acknowledgement of reboot initiation, while post-reboot sequence-list inspection proves archive extraction. Upstream source references: `https://github.com/forkineye/ESPixelStick/blob/main/src/WebMgr.cpp` (`/X6`), `https://github.com/forkineye/ESPixelStick/blob/main/src/service/FPPDiscovery.cpp` (optional `zip` system-info field and `/fpp` response), and `https://github.com/forkineye/ESPixelStick/blob/main/src/FileMgr.cpp` (ZIP discovery and unzip during startup).

## Interfaces and Dependencies

`IFppClient` gains one public asynchronous method, `UploadEspPixelStickSequenceAsync(string filename, Stream content, CancellationToken cancellationToken = default)`. `FppClient` implements it using `HttpClient`, `StreamContent`, `System.Text.Json` for the metadata response, and the existing `FppClientException`. `FppDirectUploadService` retains the existing sequence/audio/universe methods but takes the resolved target kind for its sequence upload decision. `BulkExportSummaryStage` owns target detection for each export run and passes that decision down; no serialized `ExportProfile` change is needed. Tests use xUnit, Moq, and the existing `MockHttpMessageHandler`; no new NuGet package or solution project is required.

For the revision, `FppSystemInfo` must expose the optional firmware ZIP capability. Add `public bool Zip { get; init; }` to that model; the existing case-insensitive JSON options bind lowercase `zip`, and a missing field defaults to `false`. Add these members to `IFppClient` and implement them in `FppClient`:

    Task UploadEspPixelStickArchiveAsync(string filename, Stream content, CancellationToken cancellationToken = default);
    Task RebootEspPixelStickAsync(CancellationToken cancellationToken = default);

Document both public operations and the new public property with XML comments. `FppDirectUploadService` remains the target-aware boundary and should offer archive upload and reboot only for the detected ESPixelStick path; a small archive-builder/batch component may be added under `src/Vixen.Modules/App/ExportWizard/` to keep ZIP creation independently testable. Use the platform's `System.IO.Compression.ZipArchive` and file streams; add no NuGet package. Preserve `UploadSequenceAsync` for FPP and use the existing ESPixelStick FSEQ method as the required absent/false `zip` fallback. No persisted `ExportProfile` change is needed.

Plan revision (2026-09-24, Codex): Created this plan after reading VIX-4003, the user's explicit `Platform` clarification, current Vixen docs/source/tests, and the referenced firmware handlers. The initial decisions are recorded above; revise every affected section if implementation findings change them.

Plan revision (2026-09-24 21:50Z, Codex): Completed Milestone 1 by updating the existing Jira description, then re-reading it to confirm all planned user-facing requirements, acceptance checks, and validation expectations. Updated Progress and Outcomes accordingly; no implementation design changed.

Plan revision (2026-09-24 21:59Z, Codex): Completed Milestone 2's client API and HTTP tests. Recorded the stream-ownership decision, the cancellation-test correction, actual build and test results, and the remaining wizard routing and hardware checks so the plan reflects observed behavior.

Plan revision (2026-09-24 22:04Z, Codex): Corrected the unreadable-stream test after review identified a disposed `MemoryStream` being passed to the client. A live write-only stream preserves the intended `CanRead` coverage without use-after-dispose; updated Progress, Surprises, Decision Log, and Outcomes with the correction and 15/15 upload-test result.

Plan revision (2026-09-24 22:14Z, Codex): Completed Milestone 3's wizard routing, effective summary, cancellation/failure handling, and focused service tests. A final contract check corrected the empty-platform fallback to ordinary FPP; after rebuilding, focused tests passed 24/24 and the combined FPP client/export-service tests passed 47/47. Deferred full-suite and device validation to Milestone 4.

Plan revision (2026-09-24 22:32Z, Codex): Completed Milestone 4 using the user's confirmation that the full build succeeded, all 980 unit tests passed, and manual validation succeeded. Re-read VIX-4003 and found its description already aligned with the final behavior, then posted the results in a Jira comment. The firmware version and exact command output were not provided; recorded this limitation in Progress and Outcomes.

Plan revision (2026-09-25 14:17Z, Codex): Added the new ESPixelStick-only ZIP batching and reboot requirements after the original four completed milestones. The plan now covers an always-zipped single FSEQ, ordered batches of up to five, capability detection, sequential archive uploads, one final POST `/X6` with `{"status":"Rebooting"}`, failure/cancellation behavior, test cases, and hardware verification. Existing FPP and File Path behavior remains the baseline; a target-neutral batch design leaves room for future FPP ZIP support without enabling it now. The upstream firmware's optional ZIP flag and boot-time extraction explain the capability and post-reboot checks. No implementation or new hardware validation is claimed by this revision.

Plan revision (2026-09-25 14:23Z, Codex): Clarified the new ESPixelStick progress requirement after reviewing the wizard's two `ExportProgressStatus`-driven bars. Milestone 7 and acceptance now require visible ZIP creation, each archive upload, and reboot-request phases, with overall progress reserved until the `/X6` acknowledgement and explicit failure/cancellation behavior. The earlier completed milestones are unchanged.

Plan revision (2026-09-25 14:27Z, Codex): The user clarified that older ESPixelStick firmware may omit `zip`, and both an absent property and `zip: false` must use individual, unzipped FSEQ uploads without a reboot. Revised the purpose, active milestones, architecture, progress requirements, tests, wire examples, and acceptance to distinguish this fallback from the `zip: true` ZIP-and-reboot route. The user has a verified ZIP-capable firmware version for testing. This decision supersedes the earlier planned unsupported-firmware error and unconditional ZIP behavior; completed milestone history remains intact.

Plan revision (2026-09-25, Codex): Completed the Jira portion of Milestone 5 by updating and re-reading VIX-4003 with both ESPixelStick capability routes, required batch counts and reboot behavior, progress expectations, failure acceptance, and unchanged FPP/File Path behavior. At that point, the live protocol probe was still pending because the device address was unavailable.

Plan revision (2026-09-25 14:41Z, Codex): Completed Milestone 5 after the user supplied 172.20.112.169. The device reported ESPixelStick firmware 4.0-52d8eaf0 and `zip: true`; a uniquely named empty ZIP was acknowledged with HTTP 200 and a matching `Name`, POST `/X6` returned HTTP 200 with `{"status":"Rebooting"}`, and the device returned online with reset uptime. This proves the ZIP upload and reboot acknowledgement path and basic reboot recovery, not extraction of FSEQ entries. Updated Progress, Surprises & Discoveries, Decision Log, and Outcomes accordingly.
