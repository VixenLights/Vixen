<Window x:Class="Common.ElementTagColorEditor.Views.ElementTagColorEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:commonConverters="clr-namespace:Common.WPFCommon.Converters;assembly=WPFCommon"
        xmlns:viewModels="clr-namespace:Common.ElementTagColorEditor.ViewModels"
        d:DataContext="{d:DesignInstance viewModels:ElementTagColorEditorViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008/design"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Manage Tag Colors" Height="260" Width="360" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" ShowInTaskbar="False" Background="{DynamicResource BackColorBrush}">

	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/WPFCommon;component/Theme/Theme.xaml"/>
			</ResourceDictionary.MergedDictionaries>
			<commonConverters:ColorToSolidBrushConverter x:Key="ColorToSolidBrushConverter"/>
		</ResourceDictionary>
	</Window.Resources>

	<DockPanel Margin="12">
		<StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
			<Button x:Name="SaveButton" Content="Save" Width="80" Margin="0,0,8,0" Click="SaveButton_Click" IsDefault="True"/>
			<Button x:Name="CancelButton" Content="Cancel" Width="80" Click="CancelButton_Click" IsCancel="True"/>
		</StackPanel>

		<ItemsControl ItemsSource="{Binding Tags}">
			<ItemsControl.ItemTemplate>
				<DataTemplate DataType="{x:Type viewModels:TagColorItem}">
					<Grid Margin="0,4,0,4">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>
						<TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"
						           Foreground="{DynamicResource ForeColorBrush}"/>
						<Border Grid.Column="1" Width="24" Height="24" Margin="0,0,8,0"
						        BorderBrush="{DynamicResource BorderColorBrush}" BorderThickness="1"
						        Background="{Binding Color, Converter={StaticResource ColorToSolidBrushConverter}}"/>
						<Button Grid.Column="2" Content="Choose Color..." Click="ChooseColorButton_Click"/>
					</Grid>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</DockPanel>
</Window>
