<catel:Window x:Class="VixenModules.App.CustomPropEditor.Views.XModelSelectionView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:catel="http://schemas.catelproject.com"
              Width="420"
              Height="320"
              MinWidth="360"
              MinHeight="260"
              WindowStartupLocation="CenterOwner"
              CanCloseUsingEscape="True"
              ResizeMode="CanResize"
              WindowStyle="ToolWindow">
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="..\Themes\Theme.xaml"/>
				<ResourceDictionary Source="pack://application:,,,/WPFCommon;component/Theme/Theme.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>

	<Border Padding="10" Background="{StaticResource BackColorBrush}">
		<DockPanel LastChildFill="True">
			<StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
				<TextBlock Text="Select the embedded model to import." FontWeight="SemiBold"/>
				<TextBlock Text="The Custom Prop Editor can import one model at a time." Margin="0,4,0,0"/>
			</StackPanel>

			<StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
				<Button Width="80" Margin="0,0,8,0" IsDefault="True" Command="{Binding OkCommand}">OK</Button>
				<Button Width="80" IsCancel="True" Command="{Binding CancelCommand}">Cancel</Button>
			</StackPanel>

			<ListBox ItemsSource="{Binding Models}"
			         SelectedItem="{Binding SelectedModel, Mode=TwoWay}"
			         HorizontalContentAlignment="Stretch">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<Grid Margin="2">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>
							<TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
							<TextBlock Grid.Column="1"
							           Text="{Binding ModelType}"
							           VerticalAlignment="Center"
							           Foreground="{DynamicResource DisabledForegroundBrush}"
							           Margin="12,0,0,0"/>
						</Grid>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</DockPanel>
	</Border>
</catel:Window>
