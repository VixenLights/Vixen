<catel:Window x:Class="VixenModules.App.CustomPropEditor.Views.BackgroundImageScaleWindow"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:catel="http://schemas.catelproject.com"
              Title="Scale Background Image"
              Width="440"
              Height="360"
              MinWidth="400"
              MinHeight="320"
              WindowStartupLocation="CenterOwner"
              CanCloseUsingEscape="True"
              ResizeMode="NoResize"
              WindowStyle="ToolWindow">
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="..\Themes\Theme.xaml"/>
				<ResourceDictionary Source="pack://application:,,,/WPFCommon;component/Theme/Theme.xaml"/>
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>

	<Border Padding="12" Background="{StaticResource BackColorBrush}">
		<DockPanel LastChildFill="True">
			<StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
				<Button Width="80" Margin="0,0,8,0" IsDefault="True" Command="{Binding OkCommand}">OK</Button>
				<Button Width="80" IsCancel="True" Command="{Binding CancelCommand}">Cancel</Button>
			</StackPanel>

			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="12"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="12"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,4" Text="Original bitmap:"/>
				<TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding OriginalDimensions}"/>
				<TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" Text="Current editor:"/>
				<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CurrentDimensions}"/>

				<TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,4" VerticalAlignment="Center" Text="Units:"/>
				<ComboBox Grid.Row="3" Grid.Column="1"
				          Width="120"
				          HorizontalAlignment="Left"
				          ItemsSource="{Binding Units}"
				          SelectedItem="{Binding Unit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
				<TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,4" VerticalAlignment="Center" Text="Width:"/>
				<TextBox Grid.Row="4" Grid.Column="1"
				         Width="140"
				         HorizontalAlignment="Left"
				         Text="{Binding WidthInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"/>
				<TextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,12,4" VerticalAlignment="Center" Text="Height:"/>
				<TextBox Grid.Row="5" Grid.Column="1"
				         Width="140"
				         HorizontalAlignment="Left"
				         Text="{Binding HeightInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}"/>

				<StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2">
					<CheckBox IsChecked="{Binding IsAspectRatioLocked, Mode=TwoWay}" Content="Lock aspect ratio"/>
					<CheckBox Margin="0,4,0,0"
					          IsChecked="{Binding ScaleExistingLightPositions, Mode=TwoWay}"
					          IsEnabled="{Binding HasLights}"
					          Content="Scale existing light positions"/>
					<TextBlock Margin="0,10,0,0" FontWeight="SemiBold" Text="{Binding ResultDimensions, StringFormat=Resulting size: {0}}"/>
				</StackPanel>
			</Grid>
		</DockPanel>
	</Border>
</catel:Window>
