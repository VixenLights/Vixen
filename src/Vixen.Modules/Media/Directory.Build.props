<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../../'))" />
    <PropertyGroup>
        <AssemblyName>Module.Media.$(MSBuildProjectName)</AssemblyName>
		<Nullable>disable</Nullable>
    </PropertyGroup>
</Project>


