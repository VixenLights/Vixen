<catel:Window x:Class="VixenModules.OutputFilter.CoarseFineBreakdown.Setup.Views.CoarseFineBreakdownSetupView"
		x:ClassModifier="internal"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:catel="http://schemas.catelproject.com"
		Title="Coarse Fine Breakdown Setup"
		WindowStartupLocation="CenterOwner"
		SizeToContent="Height"
		Width="420"
		MinWidth="360"
		ResizeMode="NoResize">

	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/WPFCommon;component/Theme/Theme.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Window.Resources>

	<DockPanel Background="{DynamicResource BackColorBrush}">
		<WrapPanel DockPanel.Dock="Bottom"
				   Margin="0,8,8,8"
				   Style="{StaticResource RowButtonRightContainerStyle}">
			<Button Content="OK"
					Command="{Binding OkCommand}"
					IsDefault="True"
					Style="{StaticResource RowButtonStyle}" />
			<Button Content="Cancel"
					Command="{Binding CancelCommand}"
					IsCancel="True"
					Style="{StaticResource RowButtonStyle}" />
		</WrapPanel>

		<Grid Margin="8">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<CheckBox Grid.ColumnSpan="2"
					  Margin="0,4,0,8"
					  Content="Enable default value mapping"
					  IsChecked="{Binding EnableDefaultValueMapping, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="1"
					   Grid.Column="0"
					   Margin="0,8,8,3"
					   VerticalAlignment="Center"
					   Text="Resting coarse value (0-255)" />
			<Slider Grid.Row="1"
					Grid.Column="1"
					Margin="0,8,0,4"
					Minimum="0"
					Maximum="255"
					TickFrequency="1"
					AutoToolTipPrecision="0"
					AutoToolTipPlacement="BottomRight"
					IsMoveToPointEnabled="True"
					IsSnapToTickEnabled="True"
					ToolTip="{Binding Value, RelativeSource={RelativeSource Self}, StringFormat={}{0:0}}"
					Value="{Binding RestingCoarseValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

			<TextBlock Grid.Row="2"
					   Grid.Column="0"
					   Margin="0,8,8,3"
					   VerticalAlignment="Center"
					   Text="Resting fine value (0-255)" />
			<Slider Grid.Row="2"
					Grid.Column="1"
					Margin="0,8,0,4"
					Minimum="0"
					Maximum="255"
					TickFrequency="1"
					AutoToolTipPrecision="0"
					AutoToolTipPlacement="BottomRight"
					IsMoveToPointEnabled="True"
					IsSnapToTickEnabled="True"
					ToolTip="{Binding Value, RelativeSource={RelativeSource Self}, StringFormat={}{0:0}}"
					Value="{Binding RestingFineValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		</Grid>
	</DockPanel>
</catel:Window>
