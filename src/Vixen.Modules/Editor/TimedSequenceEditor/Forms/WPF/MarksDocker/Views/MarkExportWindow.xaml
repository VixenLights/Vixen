<catel:Window x:Class="TimedSequenceEditor.Forms.WPF.MarksDocker.Views.MarkExportWindow"
              xmlns:catel="http://schemas.catelproject.com"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:TimedSequenceEditor.Forms.WPF.MarksDocker.ViewModels"
        xmlns:views="clr-namespace:TimedSequenceEditor.Forms.WPF.MarksDocker.Views"
        mc:Ignorable="d"  WindowStartupLocation="CenterOwner" 
              CanCloseUsingEscape="True" WindowStyle="ToolWindow"
        Title="Export Mark Collections" Height="400" Width="600">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WPFCommon;component/Theme/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel Background="{StaticResource BackColorBrush}">
        <WrapPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button HorizontalAlignment="Right" Width="75" Margin="6" IsDefault="True" Command="{Binding OkCommand}">Ok</Button>
            <Button HorizontalAlignment="Right" Width="75" Margin="6" IsCancel="True" Command="{Binding CancelCommand}">Cancel</Button>
        </WrapPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">
                <Label Content="Select Mark Collections To Export" FontWeight="Bold"/>
                <ScrollViewer DockPanel.Dock="Bottom" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="Black">
                    <Border Margin="1" BorderBrush="{StaticResource BackColorBrush}" BorderThickness="1">
                        <Grid Grid.IsSharedSizeScope="True">
                            <Grid.Resources>
                                <DataTemplate x:Key="MarkCollectionRow" DataType="{x:Type viewModels:MarkExportWindowViewModel}">
                                    <views:MarkCollectionExportRowView/>
                                </DataTemplate>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition SharedSizeGroup="A"/>
                                <ColumnDefinition SharedSizeGroup="B"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="{StaticResource BackColorBrush}" Grid.Column="0" BorderBrush="{StaticResource BackColorBrush}" BorderThickness="0,0,1,1" Padding="2">
                                <Image Source="/Resources;component/select-forecolor.png" Height="16" ToolTip="Include Collection"/>
                            </Border>
                            <Border Grid.Row="0" Name="TextIncludedOptionHeader" Visibility="{Binding IsTextOptionVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Background="{StaticResource BackColorBrush}" Grid.Column="1" BorderBrush="{StaticResource BackColorBrush}" BorderThickness="0,0,1,1" Padding="2">
                                <Image Source="/Resources;component/text-forecolor.png" Height="16" ToolTip="Include Mark Text"/>
                            </Border>

                            <Border Grid.Row="0" Background="{StaticResource BackColorBrush}" Grid.Column="2" BorderBrush="{StaticResource BackColorBrush}" BorderThickness="0,0,1,1" Padding="2">
                                <TextBlock Text="Collection"/>
                            </Border>

                            <ItemsControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" ItemsSource="{Binding ExportOptionsVmList}" ItemTemplate="{StaticResource MarkCollectionRow}"/>
                        </Grid>
                    </Border>
                </ScrollViewer>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical">
                <Border BorderThickness="1">
                    <StackPanel Orientation="Vertical">
                        <Label Content="Export Format" FontWeight="Bold"/>
                        <RadioButton GroupName="Group1" Content="Vixen" IsChecked="{Binding IsVixenFormat}"/>
                        <RadioButton GroupName="Group1" Content="Audacity" IsChecked="{Binding IsAudacityFormat}"/>
                        <RadioButton GroupName="Group1" Content="Pangolin Beyond" IsChecked="{Binding IsPangolinBeyondFormat}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </DockPanel>
</catel:Window>
