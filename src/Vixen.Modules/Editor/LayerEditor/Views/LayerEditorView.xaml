<catel:UserControl x:Class="VixenModules.Editor.LayerEditor.Views.LayerEditorView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:catel="http://schemas.catelproject.com"
                   xmlns:converters="clr-namespace:VixenModules.Editor.LayerEditor.Converters"
                   xmlns:utils="clr-namespace:Common.WPFCommon.Utils;assembly=WPFCommon"
                   Background="#444444">

	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/WPFCommon;component/Theme/Theme.xaml"/>
			</ResourceDictionary.MergedDictionaries>
			<converters:HasSetupVisibilityConverter x:Key="HasSetupVisibilityConverter" />
			<converters:DefaultLayerVisibilityConverter x:Key="DefaultLayerVisibilityConverter" />
		</ResourceDictionary>
	</UserControl.Resources>

	<Grid HorizontalAlignment="Stretch" Background="#444444">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
			<ColumnDefinition Width="1*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Button Grid.Column="0" Grid.Row="0" Content="Add Layer" Margin="5" Command="{Binding AddLayerCommand}" />
		<Button Grid.Column="1" Grid.Row="0" Content="Remove Layer" Margin="5" Command="{Binding RemoveLayerCommand}" CommandParameter="{Binding ElementName=_lbLayers, Path=SelectedItem}" />

		<ListBox Grid.Column="0"
		         Grid.ColumnSpan="2"
		         Grid.Row="1"
		         x:Name="_lbLayers"
		         HorizontalAlignment="Stretch"
		         VerticalAlignment="Stretch"
		         ItemsSource="{Binding Layers}"
		         Style="{StaticResource ListBoxStyle}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<StackPanel Margin="5">
						<Expander>
							<Expander.Header>
								<Grid HorizontalAlignment="Stretch">
									<Label Content="{Binding LayerName}" Padding="3" />
								</Grid>
							</Expander.Header>

							<DockPanel LastChildFill="True" Background="#444444">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto" />
										<ColumnDefinition Width="Auto" />
										<ColumnDefinition Width="Auto" />
									</Grid.ColumnDefinitions>
									<Grid.RowDefinitions>
										<RowDefinition Height="Auto" />
										<RowDefinition Height="Auto" />
										<RowDefinition Height="Auto" />
									</Grid.RowDefinitions>

									<Label Grid.Column="0" Grid.Row="0" Margin="3">Name</Label>
									<TextBox Grid.Column="1"
									         Grid.Row="0"
									         Grid.ColumnSpan="2"
									         DockPanel.Dock="Top"
									         Margin="3"
									         Text="{Binding LayerName}"
									         utils:TextBoxExtender.CommitOnEnter="true"
									         utils:TextBoxExtender.CommitOnFocusLost="true"
									         utils:TextBoxExtender.RollbackOnEscape="true"
									         utils:TextBoxExtender.CommitOnTyping="false" />

									<Label Grid.Column="0" Grid.Row="1" Margin="3" Visibility="{Binding Converter={StaticResource DefaultLayerVisibilityConverter}}">Type</Label>
									<ComboBox Grid.Column="1"
									          Grid.Row="1"
									          Grid.ColumnSpan="2"
									          Margin="3"
									          ItemsSource="{Binding ElementName=_lbLayers, Path=DataContext.FilterTypes, Mode=OneWay}"
									          DisplayMemberPath="Descriptor.TypeName"
									          SelectedValuePath="Descriptor.TypeId"
									          SelectedValue="{Binding FilterTypeId}"
									          Visibility="{Binding Converter={StaticResource DefaultLayerVisibilityConverter}}" />

									<Button Grid.Column="1"
									        Grid.Row="2"
									        Margin="3"
									        Content="Configuration"
									        Visibility="{Binding LayerMixingFilter, Converter={StaticResource HasSetupVisibilityConverter}}"
									        Command="{Binding ElementName=_lbLayers, Path=DataContext.ConfigureLayerCommand}"
									        CommandParameter="{Binding LayerMixingFilter}" />
								</Grid>
							</DockPanel>
						</Expander>
					</StackPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</catel:UserControl>
