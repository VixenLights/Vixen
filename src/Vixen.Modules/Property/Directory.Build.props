<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../../'))" />
    <PropertyGroup>
        <AssemblyName>Module.Property.$(MSBuildProjectName)</AssemblyName>
		<Nullable>disable</Nullable>
    </PropertyGroup>
</Project>


