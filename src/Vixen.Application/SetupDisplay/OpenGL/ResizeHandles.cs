namespace VixenApplication.SetupDisplay.OpenGL
{
	/// <summary>
	/// Defines the resize handles of a selected prop.
	/// </summary>
	public enum ResizeHandles
	{
		TopLeft,
		TopRight,
		BottomLeft,
		BottomRight,
	}
}
